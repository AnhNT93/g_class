<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :guest %><br>
    <%= f.text_field :guest ,class: "form-control"%>
  </div>
  <div class="field">
    <%= f.label "Incorrect answer" %><br>
    <%= f.text_area :incorrect,class: "form-control",cols: "30", rows: "15" %>
  </div>
  <div class="field">
    <%= f.label "Correct answer" %><br>
    <%= f.text_area :correct,class: "form-control",cols: "30", rows: "15" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<br>
<% end %>
